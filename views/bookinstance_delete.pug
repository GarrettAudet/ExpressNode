extends layout

block content
    h1 ID: #{book._id}

    form(method='POST')
      div.form-group
        input#bookinstance.form-control(type='hidden', name='bookinstance', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete